# 备份 TDengine 数据

:::warning
🚧 本页文档仍在撰写中，请过段时间再来看吧
:::

TDengine 数据备份可参考如下命令。

---

备份数据库：

```sh
# 创建备份文件保存目录并进入 (可选)
mkdir testbackup
cd testbackup

# 备份数据
# taosdump -A
taosdump -A -o .
taosdump -D demo -o .
```

解析备份文件：

```sh
cd taosdump.3481289097479 # 这个目录名每次都不同
taosdump -I -i ./demo.3481289595047.avro-ntb  # 解析数据文件
taosdump -I -i ./data0/demo.3481289595049.0.avro  # 解析数据文件
```

:::note
如需还原备份的 TDengine 数据库文件，请查阅[这篇文档](../restore/tdengine.md)。
:::
